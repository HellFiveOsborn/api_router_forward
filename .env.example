# Exemplo de Variáveis de Ambiente para o Route Forward

# --- Configurações do Backend ---

# Credenciais para Login no Dashboard
USER=admin
PASSWORD=changeme

# Chave Secreta JWT (!!! MUDE ISSO para uma string longa, aleatória e segura !!!)
# Use um gerador como: openssl rand -base64 32
JWT_SECRET=substitua_por_uma_chave_secreta_muito_longa_e_aleatoria_com_pelo_menos_32_chars

# Opcional: Porta para o Backend (padrão: 3001 se não definida)
# BACKEND_PORT=3001


# --- Configurações do Frontend (via Vite) ---

# URL que o Frontend usa para acessar a API do Backend
# Certifique-se de que corresponde ao host/porta onde o backend está rodando
VITE_API_URL=http://localhost:3001/api